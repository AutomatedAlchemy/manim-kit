# Host-side deps for --install / --remove (the installer protocol).
# Rendering deps live in requirements-manim.txt inside .venv.
cli-tool-kit @ git+https://github.com/Probst1nator/cli-tool-kit.git@v0.2.0
